#!/bin/bash
# postinstall script for Screenshot Renamer Uninstaller
# Delegates to uninstall.sh which handles the root context automatically.

set -e

STAGING="/usr/local/share/screenshot-renamer"

"$STAGING/uninstall.sh"

# Clean up staging area
rm -rf "$STAGING"

exit 0
